import { CheckEmailExistValidator } from './check-email-exist.validator';

describe('CheckEmailExistValidator', () => {
  it('should be defined', () => {
    expect(new CheckEmailExistValidator()).toBeDefined();
  });
});
